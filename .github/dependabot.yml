version: 2
updates:
  # The site has no runtime dependencies; only the CI actions need updating.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
